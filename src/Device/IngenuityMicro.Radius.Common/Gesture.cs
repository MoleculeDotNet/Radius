using System;
using Microsoft.SPOT;

namespace IngenuityMicro.Radius
{
    public enum Gesture
    {
        Dismiss, PageLeft, PageRight, PageUp, PageDown, ScrollDownSmall, ScrollDownLarge, ScrollUpSmall, ScrollUpLarge
    }
}
